The pricing table currently uses a custom billing toggle. Refactor
`src/components/pricing-table/index.tsx` to use Base UI's `Switch` component for
the monthly-annual toggle while preserving the current behavior and styling.
